{
  "name": "www.lamojaquera.com",
  "version": "1.0.0",
  "dependencies": {
    "@fullhuman/postcss-purgecss": "^4.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.3",
    "postcss-cli": "^8.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "hugo --gc --minify -b https://www.lamojaquera.com/",
    "serve": "hugo server --buildDrafts --buildFuture --bind 0.0.0.0"
  },
  "author": "Geert Eltink",
  "license": "MIT"
}
